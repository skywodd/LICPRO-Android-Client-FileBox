<!-- Styles declaration for the whole application. -->

<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Application theme. -->

    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Theme customizations goes here. -->
    </style>

    <!-- Generic background layout style. -->

    <style name="GenericBackgroundLayout" parent="AppTheme">
        <item name="android:paddingBottom">@dimen/activity_vertical_margin</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingTop">@dimen/activity_vertical_margin</item>
    </style>

    <!-- Login activity background layout style. -->

    <style name="LoginBackgroundLayout" parent="GenericBackgroundLayout">
        <item name="android:background">@android:color/holo_blue_light</item>
    </style>

    <!-- Login activity form layout style. -->
    <style name="LoginFormLayout">
        <item name="android:background">@drawable/login_form_background</item>
        <item name="android:paddingBottom">@dimen/activity_vertical_margin</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingTop">@dimen/activity_vertical_margin</item>
    </style>

</resources>